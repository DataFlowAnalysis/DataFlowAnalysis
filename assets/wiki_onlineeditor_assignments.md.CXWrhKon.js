import{_ as i,C as d,c as l,o as n,a2 as c,b as r,j as t,w as o,a,G as h,a3 as p}from"./chunks/framework.DDIT__tB.js";const k=JSON.parse('{"title":"Modeling Node Behaviors with Assignments","description":"","frontmatter":{},"headers":[],"relativePath":"wiki/onlineeditor/assignments.md","filePath":"wiki/onlineeditor/assignments.md"}'),g={name:"wiki/onlineeditor/assignments.md"};function u(m,e,f,b,w,v){const s=d("Mermaid");return n(),l("div",null,[e[1]||(e[1]=c('<h1 id="modeling-node-behaviors-with-assignments" tabindex="-1">Modeling Node Behaviors with Assignments <a class="header-anchor" href="#modeling-node-behaviors-with-assignments" aria-label="Permalink to &quot;Modeling Node Behaviors with Assignments&quot;">​</a></h1><p>The behavior of a node in a <a href="/wiki/dfd/">Data Flow Diagram</a> is defined by a set of assignments per output pins. The three basic operations in nodes consist of forwarding data from the input pin to the output pin, setting a <a href="/wiki/dfd.html#data-dictionary">Label</a> and removing a label. Additionally, one can describe conditional assignments that are dependent on the value of the input. For information on what label are see the <a href="/wiki/dfd.html#data-dictionary">Data Dictionary Section</a> in the wiki.</p><h2 id="addressing-incoming-labels" tabindex="-1">Addressing Incoming Labels <a class="header-anchor" href="#addressing-incoming-labels" aria-label="Permalink to &quot;Addressing Incoming Labels&quot;">​</a></h2><p>As the input pins of a node are unnamed, writing the behavior uses the names of the flows that point to the input pin of the node. For a flow named <code>foo</code> flowing into an input pin of a node, one can address the incoming labels with <code>foo</code>. If two flows named <code>foo</code> and <code>bar</code> flow into the <em>same</em> input pin, one can address the incoming labels on that input pin with <code>foo|bar</code>.</p><h2 id="forwarding-assignment" tabindex="-1">Forwarding Assignment <a class="header-anchor" href="#forwarding-assignment" aria-label="Permalink to &quot;Forwarding Assignment&quot;">​</a></h2><p>If one wants to forward data from one or multiple input pins to an output pin, one can use a <strong>Forwarding Assignment</strong>: When writing the <strong>Forwarding Assignment</strong> <code>forward &lt;InputName&gt;,...</code>, the node forwards incoming data on <code>&lt;InputName&gt;</code>. This behavior requires that the node receives data on on <code>&lt;InputName&gt;</code> and cannot be evaluated, if no data arrives.</p><div class="tip custom-block"><p class="custom-block-title">Example: Forwarding one value</p><p>Forwarding the value of data on <code>foo</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>forward foo</span></span></code></pre></div></div><div class="tip custom-block"><p class="custom-block-title">Example: Forwarding two values</p><p>Forwarding the value of data on <code>foo</code> and <code>bar</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>forward foo,bar</span></span></code></pre></div></div><div class="tip custom-block"><p class="custom-block-title">Example: Forwarding data for a pin with two different flows</p><p>Forwarding the value of data on two flows <code>foo</code> and <code>bar</code> flowing into the same pin:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>forward foo|bar</span></span></code></pre></div></div><h2 id="set-assignment" tabindex="-1">Set Assignment <a class="header-anchor" href="#set-assignment" aria-label="Permalink to &quot;Set Assignment&quot;">​</a></h2><p>If one wants to set one or multiple labels on a node regardless of the input, one can use a <strong>Set Assignment</strong> : When writing the <strong>Set Assignment</strong> <code>set &lt;Type&gt;.&lt;Value&gt;,...</code>, the node sets the list of label type <code>&lt;Type&gt;</code> and label <code>&lt;Value&gt;</code> pairs. This behavior does not require that the node receives any data on any of its input pins, and can thus be evaluated immediately, if no other assignment in the node requires otherwise.</p><div class="tip custom-block"><p class="custom-block-title">Example</p><p>Setting the label <code>Encryption.Encrypted</code></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>set Encryption.Encrypted</span></span></code></pre></div></div><h2 id="unset-assignment" tabindex="-1">Unset Assignment <a class="header-anchor" href="#unset-assignment" aria-label="Permalink to &quot;Unset Assignment&quot;">​</a></h2><p>The <strong>Unset Assignment</strong> works similarly to the <strong>Set Assignment</strong>, but unsets the given list of label type <code>&lt;Type&gt;</code> and label <code>&lt;Value&gt;</code> pairs. Whe writing the <strong>Unset Assignment</strong> <code>unset &lt;Type&gt;.&lt;Value&gt;,...</code>, the nodes specifically unsets the list of label type and label pairs. This behavior does not require that the node receives any data on any of its input pins, and can thus be evaluated immediately, if no other assignment in the node requires otherwise.</p><div class="tip custom-block"><p class="custom-block-title">Example</p><p>Unsetting the label <code>Sensitivity.Personal</code></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unset Sensitivity.Personal</span></span></code></pre></div></div><h2 id="assign-assignments" tabindex="-1">Assign Assignments <a class="header-anchor" href="#assign-assignments" aria-label="Permalink to &quot;Assign Assignments&quot;">​</a></h2><p>The <strong>Assign Assignments</strong> can express more complicated behavior of nodes. The <strong>Assign Assignment</strong> <code>assign &lt;Type&gt;.&lt;Value&gt; if &lt;Condition&gt; from &lt;InputName&gt;</code>, set the label <code>&lt;Value&gt;</code> of label type <code>&lt;Type&gt;</code> if the given <code>&lt;Condition&gt;</code> is fulfiled. The <code>&lt;Condition&gt;</code> can either be a direct boolean expression, like <code>TRUE</code> or <code>FALSE</code>, or can reference label and label types of incoming data. The incoming data that is used in the condition is defined by the <em>optional</em> <code>from &lt;InputName&gt;</code> part of the assignment, and determines that label and label types coming from <code>&lt;InputName&gt;</code> are used to evalute the <code>&lt;Condition&gt;</code>.</p><div class="tip custom-block"><p class="custom-block-title">Example: Assigning a label based on input</p><p>If one wants to set the label <code>Status.Sanitized</code> if both <code>username</code> and <code>email</code> have the label <code>Status.Sanitized</code> one can use the following <strong>Assign Assignment</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>assign Status.Sanitized if username.Status.Sanitized &amp;&amp; email.Status.Sanitized from username,email</span></span></code></pre></div></div><h2 id="pitfalls" tabindex="-1">Pitfalls <a class="header-anchor" href="#pitfalls" aria-label="Permalink to &quot;Pitfalls&quot;">​</a></h2><h3 id="input-pins-and-when-they-are-evaluated" tabindex="-1">Input Pins and when they are evaluated <a class="header-anchor" href="#input-pins-and-when-they-are-evaluated" aria-label="Permalink to &quot;Input Pins and when they are evaluated&quot;">​</a></h3><p>As described for each assignment, the assignment may not require that data is flowing into the node on a specific pin. If no assignment for a node requires that data flows into the pin, the input pin and therefore the nodes that are responsible for flowing into that pin are not evaluated. This is due to the data arriving on that input pin being irrelevant to the properties of the node. If the evaluation of the node is required, one can always set a dummy label requiring the input using an <strong>Assign Assignment</strong></p><details class="details custom-block"><summary>Forcing evaluation of nodes flowing into a pin</summary><p>For a pin named <code>pin</code> and existing label type <code>Dummy</code> with label <code>DummyValue</code> one can define the following <strong>Assign Assignment</strong> to force evaluation of the pin <code>pin</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>assign Dummy.DummyValue if TRUE from pin</span></span></code></pre></div></details><h3 id="cyclic-data-flows-and-pseudo-cycles" tabindex="-1">Cyclic Data Flows and Pseudo Cycles <a class="header-anchor" href="#cyclic-data-flows-and-pseudo-cycles" aria-label="Permalink to &quot;Cyclic Data Flows and Pseudo Cycles&quot;">​</a></h3><p>Imagine two nodes <code>A</code> and <code>B</code> with one input and output pins each that are connected with flows <code>AtoB</code> and <code>BtoA</code>. One might assume that the flow between the two nodes is cyclic, but this is not <em>nececarrily</em> true. If one of the two nodes have a behavior that does not require flows into its input pin, we can evaluate that node, then evaluate the other one. This is what we would call a <strong>Pseudo Cycle</strong>, as the flow edges look like a cycle, but the behavior specifies otherwise.</p><p>Should both nodes however require that their input pins must be evaluated, the flow is indeed cyclic and some restrictions apply:</p><ul><li>Cycles cannot be the end of a data flow though the system</li><li>Cycles cannot be the begining of a data flow though the system</li></ul><p>Asume we have the following simplified <a href="/wiki/dfd/">Data Flow Diagram</a>:</p>',27)),(n(),r(p,null,{default:o(()=>[h(s,{id:"mermaid-113",class:"mermaid",graph:"graph%20LR%3B%0AA--%3EB%3B%0AB--%3EC%3B%0AC--%3EB%3B%0AB--%3ED%3B%0A"})]),fallback:o(()=>e[0]||(e[0]=[a(" Loading... ")])),_:1})),e[2]||(e[2]=t("p",null,[a("The behavior of both "),t("code",null,"B"),a(" and "),t("code",null,"C"),a(" requires that its input pin must be evaluted. During the evaluation of cycles, we create two different flows: One flowing though the cycle once: "),t("code",null,"A->B->C->B->D"),a(" And one not flowing though the cycle: "),t("code",null,"A->B->D")],-1))])}const A=i(g,[["render",u]]);export{k as __pageData,A as default};
